# カード内コードスライドテスト

---

## 1.1: 機能とコード例
- カード1: 変数宣言
  - letで変数を宣言
  - constで定数を宣言
  ```javascript
  let count = 0;
  const MAX = 100;
  ```
- カード2: 関数定義
  - アロー関数の構文
  - 引数と戻り値
  ```typescript
  const add = (a: number, b: number): number => {
    return a + b;
  };
  ```
